# Implementation Plans

Round 1 generated by the improve skill on 2026-09-01 at commit `5860f3e`
(plans 001–015, all DONE). Round 2 generated on 2026-09-03 at commit
`f884f20` from a code-health audit (plans 016–021). Round 3 generated on
2026-09-04 at commit `42b5d56` (plan 022, user-requested feature). Round 4
generated on 2026-09-04 at commit `42b5d56` (plan 023, playwright-cli e2e
driver, user-requested). Round 5 generated on 2026-09-04 at commit `ca94562`
(plans 024–031, product-completeness round toward the "storytelling with
data" builder goal, from a product-fit audit). Execute in the order below
unless dependencies say otherwise. Each executor: read the plan fully before
starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 003  | Migrate tooltip to tipviz v3 API + fix dep declarations | P1 | M | — | DONE |
| 001  | Verification baseline (`pnpm check` + CI) | P1 | S | 003 | DONE |
| 002  | Strict characterization suite for untested core | P1 | M | 001 | DONE |
| 004  | Guard empty/all-invalid data (no silent NaN charts) | P1 | S | 002 | DONE |
| 005  | Fix tooltip listener leak (namespaced events) | P1 | S | 003 | DONE |
| 006  | Builder correctness (zoom reset, scaleExtent, withCustom) | P2 | S | 002 | DONE |
| 007  | Per-chart collision-proof extent cache | P2 | M | 002, 004 | DONE |
| 008  | Zoom extent respects margins + safe id fallback | P2 | S | 002 | DONE |
| 009  | Consolidate axis/grid/label renderers + CSS-var reader | P2 | M | 002 | DONE |
| 010  | Playwright browser smoke tests (real Chromium, UMD build) | P2 | M | 001, 004 | DONE |
| 011  | Restore lint (flat config, ported from tipviz) | P3 | M | 001 | DONE |
| 012  | DESIGN: registry-driven feature architecture | P3 | M | 002, 006, 009 | DONE |
| 013  | DESIGN: time-series axis polish | P3 | M | 009 | DONE |
| 014  | BUILD: migrate features to FeatureDefinition registry | P2 | L | 002, 006, 009, 012 | DONE |
| 015  | BUILD: ship timeTickFormat option | P2 | S | 009, 013 | DONE |
| 016  | Make points follow zoom/pan (strict TDD) | P1 | S | — | DONE |
| 017  | Dedupe render/onZoomRedraw pairs (refactor-under-green) | P2 | S | 016 | DONE |
| 018  | Decompose featureRegistry.mts (SDD) | P2 | L | 016, 017 | DONE |
| 019  | Cursor-layer theme tokens + single-sourced fallbacks (strict TDD) | P2 | M | — | DONE |
| 020  | Dead exports + duplicate Dimensions cleanup (simple edits) | P3 | S | 018 | DONE |
| 021  | AGENTS.md architecture truth-sync (simple edits) | P3 | S | 018 | DONE |
 | 022  | Data-anchored reference lines + annotations features | P1 | M | — | DONE (branch `feature/022-reference-lines-annotations`; NOT yet on main — merge is plan 027) |
 | 023  | playwright-cli-driven e2e harness (revive orphaned browser layer) | P2 | M | 001, 004 | DONE |
| 024  | Fix Date-domain fallback in ensureFiniteDomain | P1 | S | — | DONE (`advisor/024-date-domain-fix`) |
| 025  | Honest missing-data rendering (gapPolicy via line.defined) | P1 | M | 024 first | DONE (`advisor/025-gap-policy`) |
| 026  | Time-series data contract (sort, duplicates, timezone, docs) | P2 | M | 025 | DONE (`advisor/026-timeseries-data-contract`) |
| 027  | Merge 022 branch to main + e2e scenario G | P1 | S | — | DONE (`advisor/027-merge-022-e2e`) |
| 028  | Public ariaLabel + xLabel on ChartOptions | P2 | S | — | DONE (`advisor/028-public-a11y`) |
| 029  | Focus/emphasis API (withFocus) | P2 | M | — | DONE (`advisor/029-focus-api`) |
| 030  | End-of-line direct labels with collision policy | P2 | M | 024 (soft 026) | DONE (`advisor/030-end-labels`) |
| 031  | Release hygiene (vitest run, CI coverage, blocking lint) | P1 | S | last in round | BLOCKED on coverage thresholds (branches 65.95 vs 79; functions 88.09 vs 89; lines 85.26 vs 87) — needs owner decision: more tests vs lower thresholds |

## Round 5 integration

All 7 source branches merged into `advisor/round5-integration` (`def1281`):
024 → 025 → 026 → 027 → 028 → 029 → 030.

**Verification on integration branch**:
- `pnpm type-check`: exit 0
- `pnpm exec vitest run`: exit 0 — 482 tests pass (37 files)
- `pnpm lint`: exit 0
- `pnpm build`: exit 0

**Plan 031 NOT merged**: BLOCKED on coverage thresholds (numbers above). The branch
`advisor/031-release-hygiene` (`c3bd9f0`) is ready to merge once the owner decides
between writing more tests or lowering the thresholds. Note: `pnpm test` on the
integration branch still runs watch mode (the run-mode fix lives on the blocked
031 branch).

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale)

## Dependency notes

- **003 must be first.** The installed-and-resolved tipviz is v3 (per the lockfile), and `src/interactivity/tooltip.mts:223` calls `setHtml`, an API removed in tipviz v3. This currently makes 3 tooltip tests fail with `TypeError: tooltip.setHtml is not a function`, which makes `pnpm test` (and therefore `pnpm check` and every CI run) red until 003 lands. Verify order:
  1. `003` (tipviz v3 migration) — unblocks the test suite.
  2. `001` (verification baseline) — adds CI only AFTER `pnpm check` is green.
  3. `002` (strict characterization) — pins the now-green behavior so subsequent fixes are safe.
- 002 before all defect fixes (004–008): the characterization suite is the behavior contract that makes fixes safe. Fix plans flip their `it.todo` markers (004→plan-004 markers, 005→plan-005 markers) from red to green.
- 003 before 005: both edit `tooltip.mts`; migrate the API first, then fix the listener leak on the migrated code.
- 004 before 007 (extent cache) and 010 (e2e): the e2e harness asserts the empty-data guard in a real browser.
- 009 before 012/013: both designs build on the consolidated axis renderer and the safety net of 002.
- 011 can run any time after 001; it touches only config/scripts.
- 012 and 013 are design/spike plans — their deliverable is a document plus a throwaway branch, and each seeds a future numbered build plan.

### Round 2 (016–021) dependencies

- **016 first** — standalone confirmed bug (points frozen during zoom/pan because the points entry's `onZoomRedraw` at featureRegistry.mts:508-517 ignores the `newX`/`newY` the dispatcher passes). Strict TDD: failing test at the registry seam, then the minimal fix.
- 016 before 017: 017 deduplicates the render/onZoomRedraw pairs and must dedupe CORRECT behavior, and consumes 016's `pointsZoom.test.mts` as its regression guard.
- 017 before 018: do not structurally decompose a file that still carries fresh copy-paste pairs; 017's helpers stay file-local so 018's relocation is trivial.
- 019 is independent of 016–018 (different files: tooltip cursor layer, theme schema, fallback constants) — can run in parallel any time.
- 018 before 020: the decomposition decides the final export surface; 020's greps re-run against the post-018 layout. If 018 stalls long-term, 020 can run standalone (its drift note says how).
- 018 before 021: AGENTS.md must document the post-018 architecture, not today's.
- Methodology per plan: 016 strict TDD, 017 refactor-under-green, 018 SDD (spec first), 019 strict TDD, 020/021 simple edits.

### Round 3 (plan 022) notes

- **022 is standalone** — purely additive feature work (no defect, no
  dependency on unfinished plans). It picks up the DEFERRED "D3
  annotations" finding below, now that the registry (012/014/018) makes
  features one-entry additions. Semantics settled with the library owner:
  data-anchored (positions from scales, follow zoom/pan), not pixel-fixed.
- Methodology: component-first TDD (component + characterization test while
  the tree stays green, then registry wiring), zoom-seam test mirrors plan
  016's points test.

### Round 4 (plan 023) notes

- **023 repairs plan 010's orphaned layer.** 010's harness page is intact,
  but merge `107bc0d` silently dropped `@playwright/test` + the `test:e2e`
  script from `package.json` during conflict resolution — the runner
  (`tests/e2e/serve-and-run.mjs`) has been dead since. Rather than
  resurrecting the dependency, 023 switches to the globally installed
  `playwright-cli` (tipviz session pattern, per operator direction),
  deletes the dead runner, and extends coverage with `update()`/`dispose()`
  scenarios. Zero `src/` changes; manual verification layer, not wired
  into `pnpm test` or CI.
- Plan 022's features (on branch `feature/022-reference-lines-annotations`,
  not yet on `main`) are explicitly out of 023's scope; its Maintenance
  notes describe the follow-up harness chart once 022 merges.

### Round 2 outcomes

- **020 absorbed by 018.** The decomposition moved `Dimensions` consumers to `featureContext.mts`/`zoomDispatch.mts` (both re-export from `@/types/layoutTypes.mts` — single source). `FeatureOptionsMap` was deleted during the file split (zero references post-018, grep-confirmed). The plan's "un-export `axesDef`/etc." criterion is impossible post-018: `featureRegistry.mts:27-34` uses `import * as axesDefSrc from "@/chart/featureDefs/axes.mjs"` (namespace import) — TypeScript exposes no cross-module binding for non-exported consts. The defs remain `export const` but are internal-only (no re-export from `src/index.mts` or `src/internal.mts`, grep-confirmed). Net: the public-API hygiene property the plan cared about (defs not reachable from public surface) is already true.
- **019 (theme tokens) + 021 (AGENTS.md)** landed on independent branches; both verified `pnpm type-check && pnpm test --run && pnpm lint && pnpm build` clean. Tests pass at 428/428 (29/29 tooltip).

> Note (prior sequence): the original ordering had 001 first, but a pre-existing broken test suite blocks `pnpm check` from going green. Plan 003 is the true first execution; plans 001 and 002 were re-ordered to depend on it.

### Round 5 (plans 024–031) notes

Round 5 closes the gap between "library that renders lines" and "builder that
produces *Storytelling with Data*-grade time-series charts", from a
product-fit audit at `ca94562` (all top claims independently verified against
live code, including a confirmed Date-domain defect with an exact call chain).

- **024 first** — confirmed P1 defect: `ensureFiniteDomain`
  (`src/services/scales.mts:91-110`) rejects valid `[Date, Date]` domains
  (`Number.isFinite(new Date()) === false`) and silently substitutes
  `[now, now+24h]`, clipping historical data on the DEFAULT `xType: "time"`.
  Every later rendering-facing plan assumes this is fixed.
- **025 before 026** — both edit `dataWrangling.mts`; the gap policy (which
  rows survive) must be settled before the sort guarantee is added.
- **027 is the merge of already-green work** (022's branch) plus its deferred
  browser scenario. It can land any time; sooner is better — the plan index
  said DONE while main had zero annotation code.
- **028, 029 are independent** additive API surface; either order.
- **030 depends on 024** (labels anchor through the scales) and benefits from
  026 (sorted series); its collision policy answers the owner's endpoint-
  overlap design question: never render overlapping labels; `"nudge"` |
  `"hide"` | `"legend"` degradation, legend fallback is a warned consumer
  choice, never silent.
- **031 last** — config-only gate repairs (run-mode tests, enforced coverage
  thresholds, blocking lint). Its coverage step has a STOP condition: if the
  existing thresholds fail, report numbers instead of lowering them.

## Known verification blockers

- **RESOLVED (2026-09-04, plan 031)**: the earlier `.mjs`/`.mts`
  `ERR_LOAD_URL` failure is fixed by the `mjsToMtsPlugin` in
  `vitest.config.mts:7-20` — `pnpm exec vitest run` passes 429/429 at
  `ca94562`. The watch-mode hang of bare `pnpm test` is fixed by plan 031
  (`"test": "vitest run"`).
- The browser e2e layer (`tests/e2e/`) remains a MANUAL verification suite
  driven by playwright-cli CDP attach to system Chromium (Alpine/musl
  constraint: playwright's own Chromium is glibc-only). It is intentionally
  not wired into `pnpm test` or CI — see `tests/e2e/README.md`.

## Verification flow after every plan

`pnpm check` (type-check → test → build; lint appended by plan 011). CI
(from plan 001) runs the same on every push.

## Findings considered and rejected

- DIR-01 "time scale only on X, asymmetry" — REJECTED: mis-attributed. `ScaleType` already permits `"time"` on both axes via the same factory map; time-on-Y is rare and already possible.
- DIR-03 "fluent builder API doesn't exist" — REJECTED: refuted against live code. `createChart` with all `.with*()` methods, `update()` (createChart.mts:216), `dispose()` (:202) exists and is exported from src/index.mts.
- DIR-06 "no destroy/update API" — REJECTED: same refutation — `update()` and `dispose()` both exist.
- tipviz lazy-loading to shrink bundles — DEFERRED (not rejected): tipviz registers its custom element on module eval; deferring the import changes initialization order. Revisit only with dist-size measurements after 003.
- D2 area chart, D3 annotations — annotations picked up by plan 022 (Round 3); D2 area chart still DEFERRED (no user demand yet).
- DEP-02 d3 tree-shaking, DEP-03 lockfile, DX-4 README install steps, DOCS-1 api-reference accuracy — NO ACTION: verified fine during audit.
- Perf: render-context caching of computed styles beyond plan 009's `readCssNumber` — folded into 012's design space (RenderContext), not a standalone fix.

## Audit provenance

### Round 2 (plans 016–021)

Findings sourced from a code-health-audit skill pass at commit `f884f20`
(81 files, ~3.8k source lines; score 7/10 — healthy codebase, rot concentrated
in `src/chart/featureRegistry.mts`). All top claims were verified against live
code by a second read-only pass (4/4 spot-checks confirmed: points-on-zoom bug,
duplicate `Dimensions`, dead exports — with `CURVE_PRESETS`/`resolveCurve`
explicitly verified NOT dead, tooltip hardcoded visuals). Evidence is inlined
in each plan's "Current state" section.

### Round 1 (plans 001–015)

Findings sourced from a four-track audit (correctness, architecture/perf,
tests/DX/docs, dependencies/direction) at commit `5860f3e`, with the top
claims independently verified against live code (8/8 spot-checks TRUE,
three direction findings rejected as mis-attributed). Key verified
evidence is inlined in each plan's "Current state" section.
