# Pre-Context — F006: Line Rendering

> Source behavior inventory and context for Feature specification.
> Generated by `/reverse-spec` Phase 4-2.

## Runtime Exploration Results

Skipped — library-only project (no runtime UI to explore).

## Source Reference

| File Path | Role | Rebuild Target |
|-----------|------|----------------|
| `src/components/line.mts` | Line path rendering | [TBD] |
| `src/types/selectionTypes.mts` | Type definitions | [TBD] |

## Source Behavior Inventory

| ID | Source File | Function/Method | Behavior Description | Priority | Origin |
|----|-------------|----------------|---------------------|----------|--------|
| B017 | `src/components/line.mts` | `renderLine()` | Renders line path(s) using D3 line generator with configured curve interpolation | P1 | extracted |

## UI Component Features

N/A — no third-party UI component features detected

## Interaction Behavior Inventory

N/A — no interactive UI in this Feature

## Foundation Decisions

No Foundation decisions apply to this Feature (custom framework).

## Foundation Dependencies

- **Consumes from** F001: Theme curve preset
- **Consumes from** F002: Layout dimensions
- **Consumes from** F003: Bounds selection
- **Consumes from** F005: ProcessedSeries, ChartScales

## Naming Remapping

None — no original project name references.

## Static Resources

None

## Environment Variables

None

## Feature Contracts

- **Provides** line path rendering to chart output
- **Consumes from** F001, F002, F003, F005

## For /speckit.specify

**Existing Feature Summary**: Line renderer draws the data series as SVG path(s) using D3 line generator with configurable curve interpolation.

**Existing User Scenarios**:
- Single series → single line path
- Multiple series → multiple line paths (one per series)
- Different curve presets → different line interpolation styles

## For /speckit.plan

**Implementation Approach**: D3 line generator with curve interpolation from theme.

## For /speckit.analyze

**Analysis Notes**: renderLine is a pure rendering function — it reads ProcessedSeries and draws paths.
