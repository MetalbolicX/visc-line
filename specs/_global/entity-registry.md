# Entity Registry — visc-line

> Complete entity definitions for the visc-line D3.js line chart library.
> Generated by `/reverse-spec` Phase 4-1.

## Overview

| Metric | Count |
|--------|-------|
| **Total Entities** | 11 |
| **Core Entities** | 6 |
| **Type Aliases** | 5 |

## Core Entities

### ChartConfig<T>

**Type**: Generic Interface
**Owner**: F005 (Data Services)
**Source File**: `src/types/processedSeriesTypes.mts`

```typescript
interface ChartConfig<T> {
  readonly data: readonly T[];
  readonly xSerie: Readonly<{
    readonly accessor: (d: T) => unknown;
    readonly label: string;
  }>;
  readonly ySeries: readonly SeriesDescriptor<T>[];
}
```

**Fields**:
| Field | Type | Required | Description |
|-------|------|----------|-------------|
| data | readonly T[] | Yes | Array of data items |
| xSerie | Readonly<{accessor, label}> | Yes | X-axis series configuration |
| ySeries | readonly SeriesDescriptor<T>[] | Yes | Array of Y-axis series descriptors |

---

### SeriesDescriptor<T>

**Type**: Generic Interface
**Owner**: F005 (Data Services)
**Source File**: `src/types/processedSeriesTypes.mts`

```typescript
interface SeriesDescriptor<T> {
  readonly accessor: (d: T) => number;
  readonly label: string;
  readonly opacity?: number | string;
  readonly pointFill?: string;
  readonly pointRadius?: number;
  readonly pointStroke?: string;
  readonly stroke?: string;
  readonly strokeWidth?: number | string;
}
```

**Fields**:
| Field | Type | Required | Description |
|-------|------|----------|-------------|
| accessor | (d: T) => number | Yes | Function to extract Y value |
| label | string | Yes | Series label for legend |
| opacity | number \| string | No | Line opacity |
| pointFill | string | No | Point fill color |
| pointRadius | number | No | Point radius in pixels |
| pointStroke | string | No | Point stroke color |
| stroke | string | No | Line stroke color |
| strokeWidth | number \| string | No | Line stroke width |

---

### ProcessedSeries<T>

**Type**: Generic Interface (extends SeriesDescriptor<T>)
**Owner**: F005 (Data Services)
**Source File**: `src/types/processedSeriesTypes.mts`

```typescript
interface ProcessedSeries<T> extends SeriesDescriptor<T> {
  readonly data: readonly T[];
}
```

**Fields**:
| Field | Type | Required | Description |
|-------|------|----------|-------------|
| data | readonly T[] | Yes | Filtered data with valid numeric values |
| (inherited) | — | — | All fields from SeriesDescriptor<T> |

---

### Theme

**Type**: Interface
**Owner**: F001 (Theme System)
**Source File**: `src/types/themeTypes.mts`

```typescript
interface Theme {
  readonly accessibility?: Readonly<{ readonly highContrast?: boolean; readonly reducedMotion?: boolean; }>;
  readonly axis: Readonly<{
    readonly color?: string;
    readonly fontSize: number;
    readonly tickPadding: number;
    readonly tickSize: number;
  }>;
  readonly colors: Readonly<{
    readonly axis: string;
    readonly background: string;
    readonly grid: string;
    readonly palette: readonly string[];
    readonly text: string;
  }>;
  readonly grid: Readonly<{
    readonly dashArray: string;
    readonly opacity: number;
    readonly stroke: string;
    readonly strokeLinecap: string;
    readonly strokeWidth: number;
  }>;
  readonly legend: Readonly<{
    readonly fontSize: number;
    readonly itemSpacing: number;
    readonly position: "bottom" | "left" | "right" | "top";
    readonly symbolSize: number;
  }>;
  readonly line: Readonly<{ readonly curve: CurvePreset; readonly opacity: number; readonly strokeWidth: number; }>;
  readonly points: Readonly<{
    readonly fill: string;
    readonly opacity: number;
    readonly radius: number;
    readonly stroke: string;
    readonly strokeWidth: number;
  }>;
  readonly title: Readonly<{
    readonly color: string;
    readonly fontSize: number;
    readonly fontWeight: number;
    readonly padding: number;
  }>;
  readonly tooltip?: Readonly<{
    readonly background: string;
    readonly border: string;
    readonly borderRadius: number;
    readonly color: string;
    readonly fontSize: number;
    readonly padding: number;
  }>;
}
```

---

### ChartInstance<T>

**Type**: Generic Interface
**Owner**: Chart Core
**Source File**: `src/chart/chartTypes.mts`

```typescript
interface ChartInstance<T> {
  readonly container: HTMLElement;
  readonly dispose: () => void;
  readonly series: readonly ProcessedSeries<T>[];
  readonly svg: SVGSelection;
  readonly update: (newData: readonly T[]) => void;
  readonly withAxes: () => ChartInstance<T>;
  readonly withGrid: () => ChartInstance<T>;
  readonly withLegend: (options: WithLegendOptions) => ChartInstance<T>;
  readonly withPoints: () => ChartInstance<T>;
  readonly withTitle: (options: WithTitleOptions) => ChartInstance<T>;
  readonly withTooltip: (options?: WithTooltipOptions) => ChartInstance<T>;
  readonly withZoomPan: (options?: WithZoomPanOptions) => ChartInstance<T>;
}
```

---

### ChartOptions

**Type**: Interface
**Owner**: Chart Core
**Source File**: `src/chart/chartTypes.mts`

```typescript
interface ChartOptions {
  readonly curve?: CurveFactory | CurvePreset;
  readonly margins?: Margins;
  readonly theme?: Partial<Theme>;
  readonly xType?: ScaleType;
}
```

---

## Type Aliases

### AnyScale

**Type**: Union Type
**Source File**: `src/types/scalesTypes.mts`

```typescript
type AnyScale =
  | ScaleLinear<number, number>
  | ScaleLogarithmic<number, number>
  | ScalePower<number, number>
  | ScaleTime<number, number>;
```

### ScaleType

**Type**: Union Type
**Source File**: `src/types/scalesTypes.mts`

```typescript
type ScaleType = "linear" | "log" | "pow" | "time";
```

### CurvePreset

**Type**: Union Type
**Source File**: `src/types/themeTypes.mts`

```typescript
type CurvePreset =
  | "basis" | "basisClosed" | "basisOpen"
  | "bumpX" | "bumpY"
  | "cardinal" | "cardinalClosed" | "cardinalOpen"
  | "catmullRom" | "catmullRomClosed" | "catmullRomOpen"
  | "linear" | "monotoneX" | "monotoneY" | "natural"
  | "step" | "stepAfter" | "stepBefore";
```

### Margins

**Type**: Interface
**Source File**: `src/types/layoutTypes.mts`

```typescript
interface Margins {
  readonly top: number;
  readonly right: number;
  readonly bottom: number;
  readonly left: number;
}
```

### Dimensions

**Type**: Interface
**Source File**: `src/types/layoutTypes.mts`

```typescript
interface Dimensions {
  readonly width: number;
  readonly height: number;
}
```

---

## Cross-Feature Entity Sharing

| Entity | Owner | Shared With |
|--------|-------|-------------|
| ChartConfig<T> | F005 | All Features |
| ProcessedSeries<T> | F005 | F006, F007, F008, F009 |
| Theme | F001 | All Features |
| ChartInstance<T> | Chart Core | F012, F013 |
| ChartScales | F005 | F006, F007, F008, F009 |
| AnyScale | F005 | F007, F008, F009 |

---

## Validation Rules

| Entity | Rule |
|--------|------|
| ChartConfig<T> | data must be an array; ySeries must be non-empty |
| SeriesDescriptor<T> | accessor must return a number |
| ScaleType | Only "linear", "log", "pow", "time" are valid |
| CurvePreset | Must be one of 18 predefined D3 curve factories |
