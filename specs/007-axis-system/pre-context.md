# Pre-Context — F007: Axis System

> Source behavior inventory and context for Feature specification.
> Generated by `/reverse-spec` Phase 4-2.

## Runtime Exploration Results

Skipped — library-only project (no runtime UI to explore).

## Source Reference

| File Path | Role | Rebuild Target |
|-----------|------|----------------|
| `src/components/xAxis.mts` | X axis rendering | [TBD] |
| `src/components/yAxis.mts` | Y axis rendering | [TBD] |
| `src/components/axisLabel.mts` | Axis label rendering | [TBD] |

## Source Behavior Inventory

| ID | Source File | Function/Method | Behavior Description | Priority | Origin |
|----|-------------|----------------|---------------------|----------|--------|
| B018 | `src/components/xAxis.mts` | `renderXAxis()` | Renders X axis with ticks, tick labels, and domain line | P2 | extracted |
| B019 | `src/components/yAxis.mts` | `renderYAxis()` | Renders Y axis with ticks, tick labels, and domain line | P2 | extracted |
| B020 | `src/components/axisLabel.mts` | `renderXAxisLabel/renderYAxisLabel()` | Renders axis label text | P3 | extracted |

## UI Component Features

N/A — no third-party UI component features detected

## Interaction Behavior Inventory

N/A — no interactive UI in this Feature

## Foundation Decisions

No Foundation decisions apply to this Feature (custom framework).

## Foundation Dependencies

- **Consumes from** F001: Theme styling (colors, font sizes)
- **Consumes from** F002: Layout dimensions
- **Consumes from** F003: Bounds selection
- **Consumes from** F005: ChartScales

## Naming Remapping

None — no original project name references.

## Static Resources

None

## Environment Variables

None

## Feature Contracts

- **Provides** axis rendering to chart output
- **Consumes from** F001, F002, F003, F005

## For /speckit.specify

**Existing Feature Summary**: Axis system renders X and Y axes with ticks, tick labels, and optional axis labels.

**Existing User Scenarios**:
- X axis at bottom with time/value labels
- Y axis at left with numeric labels
- Labels for axis titles

## For /speckit.plan

**Implementation Approach**: D3 axis component with theme styling.

## For /speckit.analyze

**Analysis Notes**: Axes are rendered outside the content clip-path, so they are never clipped during zoom/pan.
